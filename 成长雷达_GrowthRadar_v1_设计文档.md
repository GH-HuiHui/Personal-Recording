# 成长雷达（Growth Radar）v1 设计文档

> 目标：在 iPhone 上用极低摩擦的方式记录多个长期成长方向。每完成一次相关事项，只需点击一次“+1”。系统自动形成近期雷达图，并支持“阶段归档 → 选择性继承 → 重构下一阶段”。

---

## 1. 产品定位

这不是一个复杂的习惯打卡 App，也不是能力评分系统。

它只解决三个问题：

1. 最近 7 天，我实际上把行动投入到了哪些方向？
2. 哪些方向持续发生，哪些方向正在掉线？
3. 当一个阶段结束时，我能否保留这一阶段的数据，并重新组织下一阶段的方向？

设计原则：

- **低摩擦**：一次记录尽量不超过 2 秒。
- **保留原始事实**：每次 +1 都记录成独立事件，不因后续重构而丢失。
- **阶段隔离**：旧阶段归档后保持原样；新阶段的修改不影响历史。
- **不追求六边形平衡**：雷达图用于观察行动结构，不用于评判“好坏”。
- **先简单再扩展**：v1 不加入积分商城、等级、勋章、复杂权重、AI 分析或社交功能。

---

## 2. 首版默认方向

首次启动自动创建一个“阶段 1”，包含 6 个方向：

1. 阅读
2. 英语 / 雅思
3. 技术 / 未来工作方向
4. 锻炼健身
5. 练字
6. 自媒体

用户可修改：

- 名称
- SF Symbol 图标
- 排序
- 是否保留到下一阶段

v1 建议维度数量限制：**3～8 个**。

---

## 3. 核心交互

### 3.1 首页

首页只保留最重要的信息：

顶部：
- 当前阶段名称
- 阶段开始日期
- 「近 7 日 / 本阶段」切换
- 雷达图

下方：
- 每个方向一张轻量卡片
- 卡片包含：
  - 图标
  - 名称
  - 今日次数
  - 最近 7 天总次数
  - 一个明显的 `+1` 按钮

示意：

```text
阶段 1                  近7日 | 本阶段

             [ 雷达图 ]

阅读             今日 1      +1
英语             今日 0      +1
技术             今日 2      +1
健身             今日 1      +1
练字             今日 0      +1
自媒体           今日 0      +1
```

点击 `+1` 后：

1. 立即写入一条记录。
2. 轻微触觉反馈。
3. 数字即时更新。
4. 底部出现约 3 秒的“已记录 · 撤销”提示。
5. 不弹二次确认，不进入新页面。

目标：**单次记录不超过 2 秒。**

---

## 4. 记录语义

### 4.1 每次 +1 的含义

一次 `+1` 表示：

> 我刚刚完成了一次与该方向相关、具有明确投入的有效行动。

系统不强迫用户填写时长、备注或难度。

每次点击均保留为独立记录，因此：

- 阅读一天做 3 次，可以点击 3 次。
- 技术一天做 2 次，可以点击 2 次。
- 原始次数不会被压缩。

### 4.2 为什么雷达图不用“总次数”

不同方向的任务粒度不同。

例如：
- 一次跑步可能 40 分钟；
- 一次练字可能 10 分钟；
- 技术学习一天可能出现多个独立任务。

如果直接把总次数画进雷达图，高频方向会天然占优。

因此 v1 的**主雷达图采用“活跃度”而非总次数**。

---

## 5. 雷达图指标

### 5.1 近 7 日雷达图（默认）

每个方向的数值：

> 最近 7 个自然日中，有多少天至少发生过 1 次记录。

范围固定为：

`0 ～ 7`

例：

- 阅读：5 天活跃 → 5
- 英语：3 天活跃 → 3
- 技术：4 天活跃 → 4
- 健身：3 天活跃 → 3
- 练字：2 天活跃 → 2
- 自媒体：1 天活跃 → 1

这样雷达图体现的是**持续性**，不会被某一天大量刷次数扭曲。

### 5.2 本阶段雷达图

为了让不同长度的阶段可比较，本阶段不使用绝对天数，而使用：

> 活跃天数 ÷ 阶段已进行天数 × 100%

范围：

`0% ～ 100%`

例如：

阶段已运行 20 天，阅读有 12 个活跃日：

`12 / 20 = 60%`

本阶段雷达图该轴显示 60%。

### 5.3 卡片仍展示真实次数

雷达图看持续性，卡片看实际次数。

每个方向至少展示：

- 今日次数
- 最近 7 日总次数

归档页再展示：

- 阶段总次数
- 阶段活跃天数
- 阶段活跃率

---

## 6. 阶段系统

“阶段”是整个产品最重要的数据边界。

### 6.1 一个阶段包含

- 阶段名称
- 开始时间
- 结束时间（进行中时为空）
- 状态：active / archived
- 本阶段独立的一组方向
- 本阶段全部 +1 记录

### 6.2 同一时间只允许一个 Active Stage

这样首页永远只有一个明确的当前方案。

### 6.3 结束当前阶段

入口：

`设置 / 阶段管理 → 结束本阶段`

点击后进入“阶段总结”：

```text
阶段 1
2026-09-20 ～ 2026-10-20

阅读       24 次 / 15 活跃日
英语       17 次 / 11 活跃日
技术       20 次 / 12 活跃日
健身       12 次 / 10 活跃日
练字        8 次 /  6 活跃日
自媒体      5 次 /  4 活跃日
```

操作：

- 取消
- 归档当前阶段
- 归档并创建下一阶段（推荐主按钮）

---

## 7. 创建下一阶段：选择性继承

这是 v1 的核心功能。

### 7.1 流程

用户选择：

`归档并创建下一阶段`

进入“新阶段配置”：

1. 输入新阶段名称。
2. 显示上一阶段的所有方向。
3. 每个方向前有一个勾选框：
   - 勾选 = 继承到新阶段
   - 取消 = 新阶段删除该方向
4. 可添加新方向。
5. 可修改继承方向的名称、图标、排序。
6. 确认创建。

### 7.2 数据规则

必须遵守：

> 新阶段中的方向是“复制出来的新对象”，不是复用旧方向对象。

因此：

- 阶段 1 的“英语 / 雅思”可以永久保留原名。
- 阶段 2 可以把它改为“英语口语”。
- 两个阶段互不影响。
- 阶段 1 的所有历史记录仍然能正确关联原来的方向。

### 7.3 新阶段计数归零

创建新阶段后：

- 今日数据 = 0
- 最近 7 日视图只统计新阶段开始后的数据
- 阶段数据 = 0

历史阶段单独保留。

---

## 8. 历史 / 归档页

入口：

`历史`

按时间倒序显示：

```text
阶段 3   进行中
2026-11-12 ～ 至今

阶段 2   已归档
2026-10-16 ～ 2026-11-11

阶段 1   已归档
2026-09-20 ～ 2026-10-15
```

点进归档阶段后：

- 只读
- 显示该阶段雷达图
- 各方向统计
- 阶段起止时间
- 原始方向配置
- 可查看每日记录明细
- 不允许直接修改历史方向结构

v1 可允许删除某条误记录，但建议放在“记录明细”中，不放在首页。

---

## 9. 信息架构

建议使用 3 个 Tab：

### Tab 1：今天
- 当前阶段
- 雷达图
- 六个方向 +1

### Tab 2：历史
- 阶段列表
- 阶段详情
- 原始记录

### Tab 3：设置
- 编辑当前阶段名称
- 编辑方向
- 排序
- 添加 / 删除方向
- 结束阶段
- 数据导出（可放 v1.1）

不要给首页增加额外导航层级。

---

## 10. iOS 技术方案

### 10.1 技术栈

建议：

- **Swift**
- **SwiftUI**
- **SwiftData**
- 自定义 `RadarChartView`
- 最低系统：**iOS 17**

理由：

- SwiftData 足够支撑本地结构化数据。
- 项目无需后台服务。
- SwiftUI 对这种轻量工具最合适。
- iOS 原生体验、触感反馈、深色模式和动态字体更自然。

v1 不引入第三方依赖。

### 10.2 雷达图

Swift Charts 没有原生 Radar Chart。

建议使用：

- `Canvas`
或
- 自定义 `Shape + Path`

绘制：

1. 多边形网格
2. 各轴
3. 轴标签
4. 数据多边形
5. 数据顶点

要求：

- 支持 3～8 个维度动态变化。
- 自动根据维度数量计算角度。
- 近 7 日最大值固定为 7。
- 本阶段最大值固定为 1.0 / 100%。

不要把维度名称写死。

---

## 11. SwiftData 数据模型

建议采用三个核心实体。

### 11.1 GrowthStage

```swift
@Model
final class GrowthStage {
    @Attribute(.unique) var id: UUID
    var name: String
    var createdAt: Date
    var endedAt: Date?
    var isArchived: Bool

    @Relationship(deleteRule: .cascade)
    var dimensions: [GrowthDimension]

    @Relationship(deleteRule: .cascade)
    var records: [ActionRecord]
}
```

### 11.2 GrowthDimension

```swift
@Model
final class GrowthDimension {
    @Attribute(.unique) var id: UUID
    var name: String
    var symbolName: String
    var sortOrder: Int
    var createdAt: Date

    var stage: GrowthStage?
}
```

重要：

每个 Stage 都持有自己独立的 Dimension 实例。

### 11.3 ActionRecord

```swift
@Model
final class ActionRecord {
    @Attribute(.unique) var id: UUID
    var createdAt: Date

    var stage: GrowthStage?
    var dimension: GrowthDimension?
}
```

v1 每次点击 `+1` 只创建一条 `ActionRecord`。

不要把计数直接持久化到 Dimension 中。

所有计数均由 ActionRecord 聚合计算。

好处：

- 可撤销。
- 可删除误记录。
- 可重新计算。
- 可追溯。
- 不会因为阶段重构丢失历史事实。

---

## 12. 关键统计函数

建议集中放到 `GrowthAnalyticsService`。

需要：

```swift
func todayCount(for dimension: GrowthDimension) -> Int

func recent7DayActionCount(for dimension: GrowthDimension) -> Int

func recent7DayActiveDays(for dimension: GrowthDimension) -> Int

func stageActionCount(for dimension: GrowthDimension) -> Int

func stageActiveDays(for dimension: GrowthDimension) -> Int

func stageActiveRate(for dimension: GrowthDimension) -> Double
```

注意：

所有“天”都必须通过 `Calendar.current.startOfDay(for:)` 归一化。

不要直接用 24 小时滚动窗口代替自然日。

---

## 13. +1 与撤销逻辑

### +1

```text
用户点击
→ 创建 ActionRecord
→ SwiftData 保存
→ 触觉反馈
→ UI 自动刷新
→ 展示 Undo Toast
```

### Undo

Undo 只撤销：

> 当前会话中最近一次由用户点击产生的记录。

点击撤销后直接删除该 ActionRecord。

Toast 自动在约 3 秒后消失。

避免复杂的 Undo 栈。

---

## 14. 编辑当前阶段

允许：

- 改阶段名称
- 改当前阶段 Dimension 名称
- 改图标
- 调整顺序
- 添加新方向
- 删除当前方向

### 删除有历史记录的当前方向

如果一个方向已经有记录，点击删除时不要直接 cascade 删除。

提示：

> “该方向已经有 X 条记录。为了保留历史，建议隐藏它，或结束当前阶段后在下一阶段移除。”

v1 最稳妥做法：

- 有记录：不允许硬删除，只允许“停止显示 / 停用”。
- 无记录：允许删除。

因此建议 `GrowthDimension` 增加：

```swift
var isEnabled: Bool
```

停用后：

- 首页不显示。
- 当前阶段历史统计仍保留。
- 新阶段继承时默认不勾选。

---

## 15. 首次启动

首次启动无需注册，无需教程长页。

只显示一个简短页面：

```text
成长雷达

每完成一次有效行动，就为对应方向 +1。
雷达图观察的是持续发生的方向，而不是给自己打分。

[开始]
```

点击开始：

- 自动创建“阶段 1”
- 自动创建默认 6 个方向
- 进入首页

之后用户可自行编辑。

---

## 16. 空状态

### 今日没有记录

不用显示负面文案。

建议：

> 今天还没有记录。

### 新阶段刚建立

雷达图可以为零。

文案：

> 新阶段已开始。先记录真实行动，图形会慢慢出现。

避免：

- “继续努力”
- “你落后了”
- “连续打卡中断”

产品不制造额外心理压力。

---

## 17. 视觉原则

关键词：

- 安静
- 清晰
- iOS 原生
- 不游戏化
- 不制造红点焦虑

建议：

- 使用系统背景色。
- 支持 Light / Dark Mode。
- SF Symbols。
- 不自己定义大量彩色主题。
- 雷达图保持低视觉噪音。
- `+1` 是首页最明显的交互元素。

不要在 v1 做：

- 渐变大背景
- 复杂动画
- 彩虹颜色
- 经验条
- 连续签到火焰
- 每日提醒弹窗系统

---

## 18. 建议工程结构

```text
GrowthRadar/
├── App/
│   └── GrowthRadarApp.swift
│
├── Models/
│   ├── GrowthStage.swift
│   ├── GrowthDimension.swift
│   └── ActionRecord.swift
│
├── Services/
│   ├── GrowthAnalyticsService.swift
│   └── StageService.swift
│
├── Features/
│   ├── Today/
│   │   ├── TodayView.swift
│   │   ├── DimensionCard.swift
│   │   └── RadarChartView.swift
│   │
│   ├── History/
│   │   ├── StageHistoryView.swift
│   │   └── StageDetailView.swift
│   │
│   └── Settings/
│       ├── SettingsView.swift
│       ├── EditStageView.swift
│       └── CreateNextStageView.swift
│
├── Components/
│   └── UndoToast.swift
│
└── Utilities/
    └── Date+Helpers.swift
```

v1 不需要严格 MVVM 分层。

优先保证：

- 数据模型正确
- 阶段隔离正确
- 统计正确
- UI 足够简单

---

## 19. v1 明确不做

为了避免 Codex 自动把项目做复杂：

- 不做账号。
- 不做云后台。
- 不做社交。
- 不做排行榜。
- 不做 AI 自动建议。
- 不做通知提醒。
- 不做积分、等级、徽章。
- 不做 Apple Health。
- 不做小组件。
- 不做 Shortcuts / App Intents。
- 不做自定义权重。
- 不做复杂标签。
- 不做多项目系统。
- 不做跨设备同步。

这些都可以等第一版真实使用后再决定。

---

## 20. v1.1 候选功能

只有当 v1 连续实际使用 2～4 周后，再考虑：

1. App Intent / Shortcuts：
   - “阅读 +1”
   - “健身 +1”
   - 可放主屏幕或 Action Button。

2. iCloud / CloudKit 同步。

3. 导出 CSV / JSON。

4. 月度趋势图。

5. 主屏幕 Widget。

6. 阶段备注：
   - 这一阶段为什么开始？
   - 结束时最重要的发现是什么？

7. 每个方向可选“最低有效行动”说明。

---

# 21. 验收标准

Codex 完成 v1 后，至少通过以下场景。

### 场景 A：首次启动

- 打开 App。
- 点击开始。
- 出现阶段 1。
- 默认有 6 个方向。
- 所有数字为 0。

### 场景 B：记录

- 点击“阅读 +1”。
- 今日阅读从 0 → 1。
- 最近 7 日阅读次数从 0 → 1。
- 雷达图阅读轴活跃天数从 0 → 1。
- 关闭并重启 App，数据仍存在。

### 场景 C：同一天多次记录

- 再点击阅读 +1 两次。
- 今日次数 = 3。
- 最近 7 日总次数 = 3。
- 雷达图“活跃天数”仍然 = 1。

### 场景 D：撤销

- 点击技术 +1。
- 点击底部撤销。
- 该记录被删除。
- 所有统计恢复。

### 场景 E：阶段归档

- 当前阶段已经有数据。
- 点击结束阶段。
- 选择“归档并创建下一阶段”。
- 取消勾选“练字”。
- 把“英语 / 雅思”重命名为“英语口语”。
- 创建阶段 2。

必须满足：

- 阶段 1 已归档。
- 阶段 1 仍显示“英语 / 雅思”和“练字”及其历史数据。
- 阶段 2 显示“英语口语”。
- 阶段 2 不显示“练字”。
- 阶段 2 计数全部从 0 开始。

### 场景 F：历史隔离

- 在阶段 2 修改“英语口语”为“英语听说”。
- 阶段 1 的“英语 / 雅思”不能发生任何变化。

---

# 22. Codex 实施顺序

建议不要一次生成整个 App。

按以下顺序执行，每一步运行并测试后再继续：

### Step 1
建立 Xcode SwiftUI + SwiftData 工程，以及 3 个数据模型。

### Step 2
实现首次启动 Seed：
- 如果没有 Stage，创建默认 Stage + 6 个 Dimensions。

### Step 3
实现 TodayView：
- 方向列表
- +1
- 今日次数
- 数据持久化

### Step 4
实现 AnalyticsService：
- 今日次数
- 最近 7 日次数
- 最近 7 日活跃天数
- 阶段统计

并写单元测试。

### Step 5
实现 RadarChartView。

先用静态假数据验证 3～8 个维度，再接入真实数据。

### Step 6
实现 Undo。

### Step 7
实现历史阶段列表和阶段详情。

### Step 8
实现“归档并创建下一阶段”。

重点测试：
- Dimension 深拷贝
- 历史隔离
- 新阶段归零

### Step 9
实现当前阶段编辑与 Dimension 停用。

### Step 10
统一空状态、Dark Mode、Dynamic Type、VoiceOver 基础支持。

---

# 23. 可直接给 Codex 的总提示词

下面这段可以直接复制给 Codex：

```text
请为我实现一个 iOS 17+ 原生 SwiftUI App，项目名为 GrowthRadar（成长雷达）。

这是一个极简的个人成长行动记录工具。

核心需求：
1. 用户当前有一个 Active Stage（阶段）。
2. 一个 Stage 包含 3～8 个 GrowthDimension。
3. 默认首次创建 6 个方向：
   - 阅读
   - 英语 / 雅思
   - 技术 / 未来工作方向
   - 锻炼健身
   - 练字
   - 自媒体
4. 首页每个方向只有一个明显的 +1 按钮。
5. 每点击一次 +1，就创建一条独立 ActionRecord，并永久保存在 SwiftData。
6. 首页卡片展示：
   - 今日次数
   - 最近 7 天总次数
7. 首页显示雷达图。
8. 默认“近 7 日雷达图”的每个轴不是总次数，而是最近 7 个自然日中的活跃天数，范围 0～7。一天无论点击多少次，都只贡献 1 个活跃日。
9. 可以切换“本阶段”雷达图。本阶段的值为：活跃天数 / 阶段已经历自然日数量，范围 0～100%。
10. 点击 +1 后使用轻触觉反馈，并显示约 3 秒的 Undo Toast。Undo 只撤销最后一次刚创建的 ActionRecord。
11. 支持历史阶段。
12. 用户可以结束当前阶段，并选择“归档并创建下一阶段”。
13. 创建下一阶段时：
    - 可以选择哪些旧方向继续保留
    - 可以删除某些方向
    - 可以增加新方向
    - 可以修改名称、SF Symbol 和排序
14. 新阶段中的 GrowthDimension 必须是深拷贝的新对象，绝不能复用旧 Stage 的 Dimension 对象。
15. 因此，新阶段后续修改方向名称，绝不能改变旧阶段历史。
16. 新阶段计数从 0 开始。
17. 旧阶段归档后只读，但历史数据全部保留。
18. 如果当前方向已经有 ActionRecord，不允许硬删除；改为 isEnabled = false。
19. 使用 SwiftData，不使用后端，不使用第三方依赖。
20. 雷达图请用 SwiftUI Canvas 或 Shape/Path 自定义实现，因为 Swift Charts 没有原生 Radar Chart。
21. 必须支持 3～8 个动态维度，不能把 6 个轴写死。
22. 使用 Calendar.current.startOfDay 处理所有自然日统计。
23. 支持 Light/Dark Mode、Dynamic Type 和基本 VoiceOver。
24. 不要加入账号、云服务、积分、等级、徽章、社交、通知、AI、Widget、Shortcuts 等额外功能。

数据模型至少包括：
- GrowthStage
- GrowthDimension
- ActionRecord

请优先保证：
1. SwiftData 数据关系可靠
2. 阶段历史隔离正确
3. 日期统计正确
4. UI 操作非常轻量

工程结构采用 feature-based 简单分层，不要过度 MVVM。

实现时不要一次重写全部代码。请按以下阶段推进：
1. 数据模型
2. 首次 Seed
3. +1 与 TodayView
4. AnalyticsService + 单元测试
5. RadarChart
6. Undo
7. History
8. Archive + Create Next Stage
9. Dimension 编辑
10. UI polish

每完成一个阶段先确保可以编译运行，再继续下一阶段。
```

---

## 24. 产品成功标准

第一版是否成功，不看功能数量。

实际使用两周后只判断：

1. 每次记录是否足够自然，几乎不需要思考？
2. 是否会主动打开雷达图看近期行动结构？
3. 阶段结束时，归档和重构是否真的帮助整理当前重点？

如果这三点成立，再继续扩展。

如果不成立，优先修改记录机制，而不是增加更多功能。
